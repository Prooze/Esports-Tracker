# ─── Required in production ───────────────────────────────────────────────────

# Full URL of the backend API server (no trailing slash).
# Leave unset in local dev — Vite proxies /api and /uploads to localhost:3001.
# Example: https://api.your-domain.com
VITE_API_URL=
